import * as React from 'react';
import { View, Text } from 'react-native';

export interface LoginscreenProps {
}

export function App (props: LoginscreenProps) {
    return (
      <View>
         <Text>LoginscreenProps</Text>
      </View>
    );
}
